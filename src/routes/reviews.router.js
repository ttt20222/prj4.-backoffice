// import express from 'express';
// import { createReview, getReview, updateReview, deleteReview } from '../controllers/reviews.controller.js';

// const router = express.Router();

// router.post('/:restaurantId/reviews', createReview);

// router.get('/:restaurantId/reviews/:reviewId', getReview);

// router.patch('/:restaurantId/reviews/:reviewId', updateReview);

// router.delete('/:restaurantId/reviews/:reviewId', deleteReview);

// export default router;
